<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Login</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
 <style>
/* Light Mode (default) */
body {
    font-family: 'Poppins', sans-serif;
    background-color: #f8f9fa;
     color: #f7f3f3 !important;
}

.login-card {
    max-width: 400px;
    margin: 80px auto;
    padding: 30px;
    border-radius: 12px;
    box-shadow: 0 4px 15px rgba(109, 104, 104, 0.1);
    background-color: #ffffff !important; /* force white in light mode */
    color: #fff !important;
}

input.form-control {
    background-color: #ffffff !important;
    color: #131212;
    border: 1px solid #ccc;
}

input.form-control:focus {
    border-color: #00ff88;
    box-shadow: none;
}

.btn-login {
    background-color: #00ff88;
    color: #000;
    width: 100%;
}

.btn-login:hover {
    background-color: #00e676;
    color: #000;
}

.text-link {
    color: #00ff88;
    text-decoration: none;
}

.text-link:hover {
    color: #00e676;
    text-decoration: none;
}

/* Dark Mode */
@media (prefers-color-scheme: dark) {
    body {
        background-color: #121212;
        color: #fff;
    }

    .login-card {
        background-color: #fff !important;
        box-shadow: 0 4px 15px rgba(90, 81, 81, 0.5);
        color: #000 !important;
    }

    input.form-control {
        background-color: #fefdfd !important;
        color: #0a0a0a;
        border: 1px solid #444;
    }

    input.form-control::placeholder {
        color: #aaa;
    }

    input.form-control:focus {
        border-color: #00ff88;
        box-shadow: none;
    }

    .btn-login {
        color: #000;
    }

    .text-link {
        color: #00ff88;
    }
    .page-center {
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    justify-content: center;
}
}
</style>

</head>
<body>

<div class="login-card page-center main-wrapper">
    <h2 class="text-center mb-4">Login</h2>

    <% if (error) { %>
        <div class="alert alert-danger"><%= error %></div>
    <% } %>

    <form action="/login" method="POST">
        <div class="mb-3">
            <label for="email" class="form-label">Email:</label>
            <input type="email" id="email" name="email" class="form-control" required>
        </div>

        <div class="mb-3">
            <label for="password" class="form-label">Password:</label>
            <input type="password" id="password" name="password" class="form-control" required>
        </div>

        <button type="submit" class="btn btn-login w-100 mb-3">Login</button>
    </form>

    <p class="text-center">
        Don't have an account? 
        <a href="/register" class="text-link">Register</a>
    </p>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
